/*
void print_stacks(t_stack **a, t_stack **b)
{
	t_stack *temp;

	temp = *a;
	printf("STACK A:\n");
	while(temp)
	{
		printf("ind = %d, num = %d\n", temp->index, temp->nb);
		temp = temp->next;
	}
	temp = *b;
	printf("STACK B:\n");
	while (temp)
	{
		printf("ind = %d, num = %d\n", temp->index, temp->nb);
		temp = temp->next;
	}
}
*/